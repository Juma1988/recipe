import 'package:flutter/material.dart';

class SweetsView extends StatelessWidget {
  const SweetsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sweets Page'),
      ),
    );
  }
}
