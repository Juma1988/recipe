import 'package:flutter/material.dart';

class SousesView extends StatelessWidget {
  const SousesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Souses Page'),
      ),
    );
  }
}
